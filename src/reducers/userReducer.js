const initialState = {
  // Initial state
};

export default function userReducer(state = initialState, action) {
  switch (action.type) {
    // Handle different actions
    default:
      return state;
  }
}
